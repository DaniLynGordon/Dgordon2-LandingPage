# Dgordon2-LandingPage

Dani Gordon's Landing Page — a simple static HTML site.

## Project Structure

- `index.html` — The main (and only) page of the site

## Running the Project

The project is served using Python's built-in HTTP server on port 5000:

```
python3 -m http.server 5000 --bind 0.0.0.0
```

## Deployment

Configured as a static site deployment with the root directory (`.`) as the public directory.
